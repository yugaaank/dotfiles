# ~/.zshrc — thin wrapper for ryoku zsh.
# Do not put custom aliases or plugins here; use ~/.config/zsh/user.zsh instead.
# Ryoku manages the real config in ~/.config/zsh/ryoku.zsh.

[[ -r ~/.config/zsh/ryoku.zsh ]] && source ~/.config/zsh/ryoku.zsh

alias fs="clear && fastfetch"
