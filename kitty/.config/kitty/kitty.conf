# Kitty Terminal Configuration

# --- Font Settings ---
font_size 9
font_family family="JetBrainsMono Nerd Font"
bold_font auto
italic_font auto
bold_italic_font auto

# Map box drawing characters to Noto Sans Mono for alignment
symbol_map U+2500-U+257F Noto Sans Mono

# --- UI & Styling ---
cursor_shape beam
cursor_trail 1
window_margin_width 0
window_padding_width 21.75
window_border_width 1pt
background_opacity 0.8
background_blur 1
hide_window_decorations no

# --- Shell ---
shell fish
confirm_os_window_close 0

# --- Key Bindings ---

# Copy on Ctrl+C (falls back to interrupt if no selection)
map ctrl+c copy_or_interrupt

# Search kitten binding
map ctrl+f launch --location=hsplit --allow-remote-control kitty +kitten search.py @active-kitty-window-id
map kitty_mod+f launch --location=hsplit --allow-remote-control kitty +kitten search.py @active-kitty-window-id

# Window navigation (Alt + Vim directions)
map alt+h neighboring_window left
map alt+l neighboring_window right
map alt+k neighboring_window up
map alt+j neighboring_window down

# Scrolling
map page_up scroll_page_up
map page_down scroll_page_down

# Font size adjustments
map ctrl+plus change_font_size all +1
map ctrl+equal change_font_size all +1
map ctrl+kp_add change_font_size all +1
map ctrl+minus change_font_size all -1
map ctrl+underscore change_font_size all -1
map ctrl+kp_subtract change_font_size all -1
map ctrl+0 change_font_size all 0
map ctrl+kp_0 change_font_size all 0

# Backspace deletes word on Ctrl+Backspace
map ctrl+backspace send_text all \x17

# --- Theme ---
include themes/noctalia.conf
